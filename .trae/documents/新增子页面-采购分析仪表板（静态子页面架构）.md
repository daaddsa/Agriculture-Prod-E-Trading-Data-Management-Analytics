## 目标与约束
- 目标：把你提供的“采购分析仪表板”页面框架作为一个独立子页面加入项目。
- 约束：不修改现有 `index/` 目录（数字大屏文件保持原样）；不做任何主/子页面跳转或路由连接；只新增子页面所需文件与目录。

## 目录结构（合理且不影响大屏）
- 在项目根目录新增统一的子页面容器目录：`/pages/`
- 在其下新增子页面模块目录：`/pages/procurement-dashboard/`
- 子页面内采用“结构/样式/脚本分离”的最小工程化组织：
  - `index.html`：页面骨架 + CDN 依赖引用（Vue3、ECharts）
  - `styles.css`：从你给的 `<style>` 中抽离的样式
  - `app.js`：从你给的 `<script>` 中抽离的 Vue + ECharts 初始化逻辑

## 页面文件内容处理规则
- 修正你粘贴片段中 `script src` 带反引号/空格的问题，改为合法 URL（否则浏览器无法加载）。
- 保持页面结构、布局、色彩与组件区块不变：Header / Sidebar / Content / 图表容器。
- 维持“单页可独立打开运行”的特性：直接打开 `pages/procurement-dashboard/index.html` 即可看到页面与示例图表。
- 不新增任何跳转链接逻辑：顶部导航与左侧菜单保持静态（仅作为框架展示）。

## 资源引用策略
- favicon：不复制大屏资源、不改动 `index/`，只在子页面中通过相对路径引用现有 `index/index/logoicon.png`（如果该路径在实际打开时不方便，也可先省略 favicon）。
- 依赖：继续使用 CDN 引入 Vue 3 与 ECharts 5（与你给的框架一致）。

## 验证方式（执行阶段）
- 本地直接打开 `pages/procurement-dashboard/index.html`，确认：
  - 页面正常渲染（无白屏）
  - ECharts 柱状图能初始化并在窗口 resize 时自适应
  - 样式与布局与框架一致
- 不对 `index/index/index.html` 与大屏运行做任何改动或验证以外的操作。

## 交付物
- 新增目录：`/pages/procurement-dashboard/`
- 新增文件：`index.html`、`styles.css`、`app.js`（仅包含你提供框架的等价拆分实现）