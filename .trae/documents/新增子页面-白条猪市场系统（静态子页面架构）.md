## 目标与约束
- 目标：把你提供的“第二个子页面 - 白条猪市场系统”作为独立子页面加入项目。
- 约束：不改动现有 `index/` 数字大屏目录；不做主/子页面连接与跳转；只新增该子页面所需目录与文件。

## 目录结构（沿用第一个子页面的组织方式）
- 在项目根目录 `pages/` 下新增第二个子页面模块目录：`pages/baitiao-pig-system/`
- 模块内拆分为 3 个文件（结构/样式/脚本分离）：
  - `index.html`：页面结构 + CDN 引入 Vue3、ECharts
  - `styles.css`：从你提供的 `<style>` 抽离
  - `app.js`：从你提供的 `<script>` 抽离（Vue data + ECharts 饼图初始化）

## 实现细节（保持框架一致、只做必要修正）
- 修正你粘贴片段里 `script src` 包含反引号/空格的问题，改为标准 URL（否则浏览器不会加载脚本）。
- 保持布局与样式一致：Header + main-content + top-section/bottom-section 双栏网格、卡片、表格样式。
- 保持示例数据与图表结构一致：
  - `priceData`、`transactionData` 放在 Vue `data()` 中
  - `mounted()` 中调用 `initPieChart()`
  - `#pieChart` 容器渲染环形饼图并监听 resize
- favicon：不复制任何资源，不动大屏目录；在子页面中复用现有 `index/index/logoicon.png` 的相对路径（若路径在某些打开方式下不通，可先去掉 favicon 行）。

## 验证方式（执行阶段）
- 通过本地预览/静态访问确认：
  - `pages/baitiao-pig-system/index.html` 可独立打开
  - 页面布局正常（表格/统计卡片/饼图/查询表格完整显示）
  - ECharts 饼图可渲染且 resize 正常
- 不对 `index/index/index.html` 或任何大屏文件做改动。

## 交付物
- 新增目录：`pages/baitiao-pig-system/`
- 新增文件：`index.html`、`styles.css`、`app.js`（仅包含你提供框架的等价拆分实现）