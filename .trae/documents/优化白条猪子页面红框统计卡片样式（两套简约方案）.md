## 现状分析（红框区域定位）

* 红框区域对应第二个子页面的统计卡片网格：`pages/baitiao-pig-system/index.html` 中的 `.stats-grid` + `.stat-card` + `.stat-label/.stat-value`（约在第 46-72 行）

* 当前样式在 `pages/baitiao-pig-system/styles.css` 中：

  * `.stats-grid` 2 列网格（gap 15px）

  * `.stat-card` 基础白底 + 阴影 + 圆角 8px；随后 `.stat-card.teal/orange/gray` 直接改为高饱和渐变背景 + 白字

* 主要问题（从截图观感）

  * 卡片阴影与圆角偏“通用模板”，层次不够精致；色块较硬、缺乏细节（高光/边框/hover 反馈）

  * 长文本（如“LOS单价差…”）在小屏时易显拥挤

  * 与页面其它白底卡片（表格、图表卡片）在“精致度/统一感”上有提升空间

## 目标

* 保持主题一致（沿用现有主色：teal/orange/gray，字体与间距体系不变），整体简约

* 重点优化：圆角、边框/高光、阴影层级、文字排版与响应式

* 提供至少两套方案，可一键切换对比

## 方案 ：白底极简卡 + 主题色强调（更简约、更统一）

* 设计要点

  * 卡片背景统一白色，与页面其它 card 更一致

  * 使用“主题色强调条/描边”（例如顶部 4px 色条或左侧色条）表达 teal/orange/gray 分类

  * 文本使用深色，数值用主题色突出

* 美观提升点

  * 页面整体更“干净”，减少大面积高饱和色块带来的压迫感

  * 与表格卡片、图表卡片风格统一，界面更专业

## 实现方式（允许两套方案共存，便于选择）

* 修改文件：

  * 更新 `pages/baitiao-pig-system/styles.css`：新增/调整 `.stats-grid`、`.stat-card`、`.stat-label`、`.stat-value` 以及媒体查询

  * 轻量修改 `pages/baitiao-pig-system/index.html`：在 `<body>` 上增加一个可选属性用于切换方案，例如：

    * 默认不加属性 = 方案 A

    * `data-stat-skin="b"` = 方案 B

* 不改动任何 `index/` 数字大屏目录与文件

## 响应式与跨浏览器验证（执行阶段）

* 响应式：补充断点

  * 中屏：保证 `.top-section` 在窄屏时自动变为单列，避免统计区被压扁

  * 小屏：`.stats-grid` 从 2 列变 1 列，长文案可换行且不溢出

* 浏览器验证：

  * 使用本地预览分别检查 Chrome/Edge/Firefox（以及移动端模拟）中：卡片圆角、阴影、hover、文字换行与布局无错位

## 交付物（执行完成后输出）

* 直接给出两套方案对应的 CSS 代码片段（A 与 B）

* 给出“对比截图”交付：

  * 优先：尝试用本地预览页面生成 before/after 截图（若环境可用 headless 浏览器）

  * 备选：若无法自动截图，提供可在浏览器中一键对比的切换方式 + 明确的截图路径与操作步骤（保证你能快速拿到对比图）

